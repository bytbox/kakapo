For 0.3:
 * Macros
 * Goroutines
 * Better compat.go (e.g. callbacks)
 * Variadic function arguments
 * Remove as many builtins as possible

Other:

Ability to write current state to a file
Use gmp for math where necessary (or everywhere)
Nicer terminal interaction in REPL
Test suites: measure code coverage
benchmarks
Clean up scanpkgs code
better error handling
proper documentation
unquote
(define (f x) x) syntax
